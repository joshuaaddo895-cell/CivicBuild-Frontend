# Base URL for the Spring Boot backend (no trailing slash).
# Copy to .env.development or .env.production (both gitignored) — never commit real URLs or secrets here.
# Local LAN example: http://192.168.x.x:8081
# Production (Railway) — required for EAS builds and physical device testing:
EXPO_PUBLIC_API_URL=https://civicbuild-production.up.railway.app
# Local backend dev only:
# EXPO_PUBLIC_API_URL=http://localhost:8081
EXPO_PUBLIC_APP_ENV=development
# Optional: default API timeout (ms) and upload timeout (ms, default 60000)
# EXPO_PUBLIC_API_TIMEOUT_MS=10000
# EXPO_PUBLIC_UPLOAD_TIMEOUT_MS=60000
EXPO_PUBLIC_PROJECT_ID=your-eas-project-id
# Checkout uses mock Paystack flow only when explicitly enabled.
EXPO_PUBLIC_USE_MOCK_CHECKOUT=false
# Google OAuth — Web client ID is required (backend verifies `aud` against this).
# Register redirect URI: https://auth.expo.io/@YOUR_EXPO_USERNAME/civicbuild
EXPO_PUBLIC_EXPO_OWNER=your-expo-username
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=
# Required for `npm run start:tunnel` — install ngrok v3 CLI first: winget install ngrok.ngrok
# Token: https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTHTOKEN=
