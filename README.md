# CivicBuild Frontend

## Project Overview

CivicBuild-Frontend is the mobile/web frontend for the CivicBuild platform, designed to connect local communities with essential services through an easy, fast, and secure marketplace experience.

## Git config commands for each contributor

Use these inside the repo directory to set the local commit author for each person:

```powershell
git config user.name "A.Y.G.FMemphis"
git config user.email "melingamemphis@gmail.com"
```

```powershell
git config user.name "Jesscute389"
git config user.email "jessicaowusu389@gmail.com"
```

```powershell
git config user.name "Browns-lab"
git config user.email "brownkwame323@gmail.com"
```

```powershell
git config user.name "Steph5123"
git config user.email "Stephanappiah724@gmail.com"
```

```powershell
git config user.name "einsteinbasit234-maker"
git config user.email "einsteinbasit234@gmail.com"
```

```powershell
git config user.name "joshuaaddo895-cell"
git config user.email "joshuaaddo895@gmail.com"
```

> If they want it globally for their machine instead of just this repo, add `--global` after `git config`.
